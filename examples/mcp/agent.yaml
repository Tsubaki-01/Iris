name: iris-example-mcp
model: deepseek/deepseek-chat
system: |
  你是本地 MCP 示例助手。用户要求回显文本时，调用 mcp__local__echo 并返回结果。
mcp:
  path: mcp.json
  overrides:
    local:
      trust_annotations: true
permissions:
  workspace: .
